"use client";
import { useTextToSpeech } from "../hooks/useTextToSpeech";
export default function VoiceNarrationPlayer({text}:Readonly<{text:string}>){const speech=useTextToSpeech(text);if(!speech.supported)return <p className="section-note">Voice narration is unavailable in this browser. The transcript remains available above.</p>;return <div className="voice-player"><button onClick={speech.play}>{speech.paused?"Resume":speech.playing?"Playing…":"▶ Play"}</button><button onClick={speech.pause} disabled={!speech.playing}>Pause</button><button onClick={speech.restart}>Restart</button><button onClick={speech.stop}>Stop</button><label>Speed<select value={speech.rate} onChange={event=>speech.setRate(Number(event.target.value))}>{[.75,1,1.25,1.5,2].map(rate=><option key={rate} value={rate}>{rate}x</option>)}</select></label><label>Voice<select value={speech.voice} onChange={event=>speech.setVoice(event.target.value)}><option value="">Default</option>{speech.voices.map(item=><option key={item.name} value={item.name}>{item.name}</option>)}</select></label></div>}
