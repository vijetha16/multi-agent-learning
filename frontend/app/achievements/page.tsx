"use client";
import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import PortalNav from"../components/PortalNav";import{api,getToken}from"../lib/api";import"../portal.css";
type Achievement={id:string;name:string;description:string;icon:string;unlocked:boolean;progress:number;goal:number};
export default function Achievements(){const router=useRouter();const[data,setData]=useState<Achievement[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{try{if(!getToken()){router.replace("/auth");return}setData(await api<Achievement[]>("/achievements"))}finally{setLoading(false)}})()},[]);
if(loading)return<main className="portal-loading"><img src="/lumi-guide.png" alt="Lumi"/><p>Polishing your badge shelf…</p></main>;
const earned=data.filter(item=>item.unlocked).length;return<main className="portal-shell"><PortalNav/><section className="portal-main"><header className="portal-head"><div><small>YOUR MILESTONES</small><h1>Achievement gallery.</h1><p>You have unlocked {earned} of {data.length} badges. Every badge reflects real progress saved in your learning history.</p></div><div className="portal-balance">★ {earned}/{data.length} earned</div></header><div className="achievement-grid">{data.map(item=><article className={`surface achievement-card ${item.unlocked?"":"locked"}`} key={item.id}><span className="badge-icon">{item.unlocked?item.icon:"🔒"}</span><div><small>{item.unlocked?"UNLOCKED":"IN PROGRESS"}</small><h2>{item.name}</h2><p>{item.description}</p><div className="badge-progress"><i style={{width:`${Math.min(100,item.progress/item.goal*100)}%`}}/></div><small>{item.progress} / {item.goal}</small></div></article>)}</div><article className="surface feature-strip"><img src="/lumi-guide.png" alt="Lumi"/><div><h2>Mastery DNA</h2><p>A living skill profile grows from the evidence behind your badges—not just time spent, but lessons finished, quiz performance, consistency, and complete projects.</p></div><button>Updates automatically</button></article></section></main>}
